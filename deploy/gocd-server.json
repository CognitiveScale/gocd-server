consul = "consul.service.consul:8500"
log_level = "info"

vault {
  address = "https://consul.service.consul:8200"
// token is from VAULT_TOKEN env variable..
  renew = true
  ssl {
    enabled = true
    verify  = true
    cert    = "/etc/consul/ssl/consul.cert"
    ca_cert = "/etc/consul/ssl/ca.cert"
  }
}

template {
  source = "/consul-template/templates/id_rsa.ctmpl"
  destination = "/root/.ssh/id_rsa"
  command = "ssh-keyscan github.com >> /root/.ssh/known_hosts"
  perms = 0600
}
